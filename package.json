{
  "name": "dq-payment-page",
  "version": "1.0.0",
  "description": "DQ Program payment page — Stripe embedded checkout (TypeScript)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@stripe/stripe-js": "^4.10.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
