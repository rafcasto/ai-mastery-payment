# Base URL of the Stripe/Ghost integration API.
# The browser calls these endpoints cross-origin, so it sends an Origin header
# that the integration backend must whitelist for THIS deployment's domain.
VITE_API_BASE=https://stripe-ghosts-custome-checkout.vercel.app
