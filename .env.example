# Copy to .env.local for development overrides.
# Base URL of the Stripe/Ghost integration API (GET /api/config, /api/products,
# POST /api/sessions/create, GET /api/coupons/:code). Leave empty to use
# same-origin relative requests.
VITE_API_BASE=https://stripe-ghosts-custome-checkout.vercel.app
